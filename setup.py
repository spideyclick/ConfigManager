from setuptools import setup

setup(
    name = 'configManager',
    version = '1.0.1',
    description = 'A package for managing configuration files and interpolating variables/secrets',
    url = 'https://github.com/spideyclick/ConfigManager',
    author = 'spideyclick',
    author_email = 'spideyclick@gmail.com',
    license = 'MIT',
    packages = ['configManager'],
    install_requires = [],
)